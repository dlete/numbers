<%= form_for(@cash_receipt) do |f| %>
  <% if @cash_receipt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cash_receipt.errors.count, "error") %> prohibited this cash_receipt from being saved:</h2>

      <ul>
      <% @cash_receipt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :concept %><br />
    <%= f.text_field :concept %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :account %><br />
    <%= f.collection_select :account_id, @accounts,
                            :id, :name,
                            :include_blank => true %>

  </div>
  <div class="field">
    <%= f.label :posted %><br />
    <%= f.check_box :posted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
